# Select All But Title

Obsidian plugin that adds a command for three-step selection:

1. First run selects the current paragraph, excluding Markdown bullet, ordered-list, and task checkmark markers.
2. Running the command again while that paragraph is selected selects everything under the current heading and above the next heading.
3. Running the command again while that heading body is selected selects all note text except the topmost # heading.
